{
  "servers": {
    "duckduckgo": {
      "command": "docker",
      "args": ["run", "-i", "--rm", "mcp/duckduckgo"],
      "env": {},
      "description": "DuckDuckGo search server for web searches"
    }
  },
  "gateway": {
    "port": 8080,
    "host": "0.0.0.0"
  }
}
